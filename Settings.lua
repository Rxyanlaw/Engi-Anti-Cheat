local Settings = game.ServerScriptStorage.EngiAntiCheat.Main.Settings
game:GetService("ReplicatedStorage")
if game.(path.to.EngiAntiCheat).Directory == Workspace then
  print("EngiAntiCheat is somehow not running on ServerSccriptStorage!")
  return "Settings"
